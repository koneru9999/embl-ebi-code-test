spring:
  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate
  datasource:
    password: sa
    username: sa
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
  flyway:
    enabled: true

server:
  port: 8080